# Assignment 8 — Remaining TODO

## main.c

- [ ] Add `#include <linux/slab.h>` — needed for `krealloc` and `kfree` (will not compile without it)

## Build & Test

- [ ] Run `make` in `aesd-char-driver/` to verify compilation
- [ ] Run `drivertest.sh` to validate driver behavior
- [ ] Run `sockettest.sh` to validate socket → driver integration
