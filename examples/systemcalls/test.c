#include <stdio.h>
#include "systemcalls.h"

int main(void)
{
    while(1)
    {
        char* cmd = "";
        printf("Please Enter your command:\t");

        scanf("%s", cmd);
    }

    return 0;
}